services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: ubuntu
      POSTGRES_PASSWORD: myplace
      POSTGRES_DB: myplace
    ports:
      - "5432:5432"
    volumes:
      - myplace_pg:/var/lib/postgresql/data

volumes:
  myplace_pg:
